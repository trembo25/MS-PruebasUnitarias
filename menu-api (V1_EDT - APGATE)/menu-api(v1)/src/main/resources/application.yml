server:
  port: 8081

spring:
  application:
    name: gestionmenu

  datasource:
    url: jdbc:mysql://localhost:3306/orderflow-v1-m2
    username: root
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Configuración de Eureka Client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/