spring:
    application:
        name: gestionreservas

    datasource:
        url: jdbc:mysql://localhost:3306/orderflow-v1-m9
        username: root
        password: ""

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true

    doc:
        api-docs:
            enabled: true
            version: openapi_3_0
        swagger-ui:
            enabled: true

server:
    port: 8088

# Configuración de Eureka Client
eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/
